version: 2

sources:
  - name: RAW_ZELDA
    schema: RAW_ZELDA
    tables:
      - name: raw_sessions
      - name: raw_achievements
      - name: raw_characters
      - name: raw_game_character
      - name: raw_games
      - name : raw_items
      - name : raw_locations
      - name : raw_players
      - name: raw_player_achievements
      - name : raw_reviews
models:
  - name: ODS_ZELDA_GAMES
    columns:
      - name: game_id
        tests:
          - not_null
          - unique
